const fs=require('node:fs'),assert=require('node:assert/strict');const html=fs.readFileSync('index.html','utf8'),script=fs.readFileSync('passwords.js','utf8'),sw=fs.readFileSync('sw.js','utf8');
for(const id of ['passwordsNav','passwordsPage','passwordBoard','passwordDetailHost','passwordDetailDialog','passwordFileId','passwordSiteDialog','credentialDialog','passwordConflictDialog'])assert(html.includes(`id="${id}"`),`missing ${id}`);
for(const id of ['contactsNav','contactsPage','contactBoard','contactDetailHost','contactDetailDialog','companyDialog','personDialog','personPhones','personDocuments'])assert(html.includes(`id="${id}"`),`missing ${id}`);
for(const id of ['smallCardsNav','smallCardsPage','smallCardWall','smallCardDialog','smallCardType','smallCardText','smallCardImageInput','smallCardImageFolderId'])assert(html.includes(`id="${id}"`),`missing ${id}`);
for(const id of ['companyUrl','companyPhone','companyAddress'])assert(html.includes(`id="${id}"`),`missing ${id}`);
assert(html.indexOf('passwords.js?v=20260923-1')<html.indexOf('ui.js?v=20260923-1'));assert(sw.includes('passwords.js?v=20260923-1'));assert(html.includes('@media(max-width:820px)')&&html.includes('.password-detail-host{display:none}'));
assert.ok((script.match(/class="password-record-row"/g)||[]).length>=4);assert(script.includes('target="_blank"')&&script.includes('rel="noopener noreferrer"'));assert(script.includes('cloud.readCloudFile')&&script.includes('cloud.writeCloudFile'));assert(script.includes('data-password-category-name'));assert.equal((script.match(/data-password-copy=/g)||[]).length,2);assert(script.includes('navigator.clipboard.writeText')&&script.includes('classList.add("copied")'));
assert(script.includes('contactCategoryNames')&&script.includes('contactCategories'));assert(script.includes('data-contact-add-row')&&script.includes('data-contact-field-name')&&script.includes('data-contact-field-value'));
assert(script.includes('smallCards')&&script.includes('data-small-card-copy-text')&&script.includes('data-small-card-copy-image')&&script.includes('data-small-card-copy-text-image'));assert(script.includes('new ClipboardItem')&&script.includes('textCardBlob'));
console.log('PASS password, contacts and small-card navigation, shared storage, company details, responsive cards and clipboard actions');
